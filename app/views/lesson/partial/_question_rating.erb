<div class="rating">
  <%= javascript_tag("document.write(getTags_question_rating('" << question_row.id.to_s << "'," <<
                             (question_row.rating_average.nil? ? 0 : question_row.rating_average * 25).to_s << "," <<
                             (!question_user_rated(question_row.id, session['user_id'])).to_s << "));") %>
</div>
