<% content_for :title do %>iosie VISITOR - View Lesson<% end %>
    <style>
        .proto {
            display: none;
        }

        .cross {
            position: absolute;
            top: -19px;
            right: -19px;
            display: block;
            background: url('cross.png') no-repeat 0 0;
            width: 36px;
            height: 35px;
        }

        .cross:hover {
            background: url('cross.png') no-repeat 0 -36px;
        }
    </style>
    <script type="text/javascript">
        $(function () {
            //Tabs
            $("#tabs").tabs();
        });
    </script>

<!-- /-- Body -->

<%= render :partial =>'shared/feedback' %>

<%= render :partial =>"shared/header" %>

<div id="content" class="container_24 clearfix">
  <%= render :partial =>"lesson/partial/glossary" %>

  <%= render :partial =>"lesson/partial/lesson_header" %>

  <%= render :partial =>"lesson/partial/sidebar" %>

  <%= render :partial =>"lesson/partial/tabbed_area" %>
</div>

<%= render :partial =>"shared/footer" %>

<%= render :partial =>"shared/content_suggestion" %>

<!-- \-- Body -->
